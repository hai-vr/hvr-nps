Core classes
=====

These are considered to be core classes, and they must be able to be easily converticle to run in the Cilbox environment,
which does not support generics at this time.

Therefore, those classes must not use generics.

The content of these classes will be copied almost verbatim to the CilboxVariants folder.
